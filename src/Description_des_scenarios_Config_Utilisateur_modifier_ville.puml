@startuml
autonumber
skinparam sequenceMessageAlign center
skinparam actorStyle hollow

actor "Utilisateur" as user
participant "Application" as app

note over user, app
L’utilisateur inscrit et authentifié
end note

user -> app : Demande d’accès au profil
app -> app : Charger infos actuelles
app -> user : Affiche les infos actuelles
user -> app : Sélectionne "Modifier ville"
user -> app : Soumet code Postale
app -> app : Recherche correspondance avec ville existante

loop tant que la ville n'est pas reconnu
alt  L'app connait la ville
app -> app : Enregistre nouvelle ville utilisateur
app -> user : Ville  mis à jour
else  L'app ne connait pas la ville
app -> app : L'app ne reconnait pas la ville avec le code Postale
app -> user : Affiche message d'erreur
app -> user : redemande la ville
user -> app : refait la procedurep

end
end

@enduml
